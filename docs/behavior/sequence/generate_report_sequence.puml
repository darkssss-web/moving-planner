@startuml Генерация_Отчёта_По_Упаковке

actor "Пользователь" as User
participant "Интерфейс" as UI
participant "Сервис отчётов" as ReportService
participant "PDF сервис" as PDF
database "База данных" as DB

User -> UI : создать отчёт
UI -> ReportService : сформировать отчёт
activate ReportService

ReportService -> DB : получить список вещей
activate DB
DB --> ReportService : список вещей
deactivate DB

loop для каждой вещи
  ReportService -> ReportService : добавить в отчёт
end

opt Экспорт в PDF
  ReportService -> PDF : сгенерировать PDF
  activate PDF
  PDF --> ReportService
  deactivate PDF
end

ReportService --> UI : отчёт готов
deactivate ReportService

@enduml
